# Personal website

Read and follow `AGENTS.md`. Before other repository work, run
`node .agents/session-context/context-loader.mjs load start`. After context compaction or handoff,
run the loader with `resume` before continuing.
