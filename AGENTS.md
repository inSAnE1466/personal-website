# Personal website

This repository owns Fred's personal writing and social proof surface. Keep
owner writing, company claims, and publication authority separate as described
in `README.md`.

## Agent intake and coordination

Read `AGENTS.md` and `product-tracker.md` on every new context and after compaction. The tracker is
a pointer index, not an agent log. Put durable writing and publication rules in `README.md` or an
owned document, and add or change a tracker line only when a pointer changes. If a document becomes
obsolete, remove its pointer and delete it after surviving rules move to current authority. Use
native OMP `hub` or message tools when active agents overlap. Do not create a message board.

## pstack verification
Route non-trivial work through the workspace pstack skill at
/Users/fredcaseyhousand/Desktop/github/tinymacro-workspace/.agents/skills/pstack/SKILL.md.
Use `.agents/skills/verify-personal-markdown-blog/SKILL.md` for the real build
and browser gate. Verification may preview drafts locally but must not push
`main`, dispatch Pages, or publish the site. 
